module.exports = (sequelize, Datatypes) => {
    const Menu = sequelize.define('Menu', {
        name:
        type: // food, drink, etc.
        category: //Favourites, Recommended, dessert, snack, drink,

    })
}
